name: precisometrologia_app
description: Aplicativo Oficial para os Funcionários da Preciso Tecnologia.

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# Read more about versioning at semver.org.
version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  intl: ^0.15.7
  cloud_firestore: 0.8.2+3
  firebase_auth: 0.6.6
  device_info: 0.3.0
  shared_preferences: 0.4.3
   connectivity: 0.3.2
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.6.1

flutter_icons:
  android: true 
  ios: "false"
  image_path: "images/logo/precisoicon.png"
  adaptive_icon_background: "#FAFAFA"
  adaptive_icon_foreground: "images/logo/precisometrologia.png"

flutter:
  uses-material-design: true
  assets:
  - images/logo/precisometrologia.png